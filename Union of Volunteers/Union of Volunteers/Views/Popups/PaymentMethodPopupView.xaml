<UserControl x:Class="Union_of_Volunteers.Views.Popups.PaymentMethodPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Union_of_Volunteers.Views.Popups" xmlns:popups="clr-namespace:Union_of_Volunteers.ViewModels.Popups" d:DataContext="{d:DesignInstance Type=popups:PaymentMethodViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid Background="White" Height="684" Width="1755" VerticalAlignment="Center" HorizontalAlignment="Center">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel Orientation="Vertical" >
                    <Image Source="/Resources/Images/PaymentMentmethodIcon.png" Height="175" Width="175" />
                    <TextBlock Text="Выберите способ пожертвования" />
                    <TextBlock Text="{Binding Price}" />
                </StackPanel>

                <StackPanel Orientation="Vertical">
                    <Button Content="Картой" Command="{Binding CardMethodCommand}" Width="762" Height="120" />
                <Button Content="По QR коду" Command="{Binding QrMethodCommand}" Width="762" Height="120" />
                    <Button Content="Отмена" Command="{Binding ExitPopupCommand}" Width="762" Height="120" />
                </StackPanel>
            </StackPanel>

            
    </Grid>
</UserControl>
