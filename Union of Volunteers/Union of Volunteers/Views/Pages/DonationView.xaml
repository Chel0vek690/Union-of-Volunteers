<UserControl x:Class="Union_of_Volunteers.Views.Pages.DonationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Union_of_Volunteers.Views.Pages" 
             xmlns:pages="clr-namespace:Union_of_Volunteers.ViewModels.Pages" 
             d:DataContext="{d:DesignInstance Type=pages:DonationPageViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Button Content="Назад" Command="{Binding CancelButtonCommand}" Height="80" Width="212" />
        <StackPanel>
            <TextBlock Text="Выберите проект" />
            <ComboBox Height="30" Width="500"  ItemsSource="{Binding Projects}" SelectedItem="{Binding SelectedProject}" >
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding title}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
                <TextBlock Text="Укажите сумму пожертвования" />
            <StackPanel Orientation="Horizontal">
                <RadioButton GroupName="Sum" Content="100 ₽" IsChecked="{Binding RadioButton100}" />
                <RadioButton GroupName="Sum" Content="500 ₽" IsChecked="{Binding RadioButton500}" />
                <RadioButton GroupName="Sum" Content="1000 ₽" IsChecked="{Binding RadioButton1000}" />
                <RadioButton GroupName="Sum" Content="5000 ₽" IsChecked="{Binding RadioButton5000}"  />
            </StackPanel>
            <TextBlock Text="{Binding OwnAmount}" />
            <WrapPanel Width="294" Height="397">
                <Button Content="1" Height="88" Width="88" Command="{Binding Number1Command}" />
                <Button Content="2" Height="88" Width="88" Command="{Binding Number2Command}" />
                <Button Content="3" Height="88" Width="88" Command="{Binding Number3Command}" />
                <Button Content="4" Height="88" Width="88" Command="{Binding Number4Command}" />
                <Button Content="5" Height="88" Width="88" Command="{Binding Number5Command}" />
                <Button Content="6" Height="88" Width="88" Command="{Binding Number6Command}" />
                <Button Content="7" Height="88" Width="88" Command="{Binding Number7Command}" />
                <Button Content="8" Height="88" Width="88" Command="{Binding Number8Command}" />
                <Button Content="9" Height="88" Width="88" Command="{Binding Number9Command}" />
                <Button Content="0" Height="88" Width="88" Command="{Binding Number0Command}" />
                <Button Content="Cancel" Height="88" Width="191" Command="{Binding ButtonBackspaceCommand}" />
            </WrapPanel>
            <StackPanel Orientation="Horizontal">
                <Button Content="Отмена" Command="{Binding CancelButtonCommand}" Width="388" Height="120" />
                <Button Content="Пожертвовать" Command="{Binding GoToPaymentMethodCommand}" Width="388" Height="120" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
