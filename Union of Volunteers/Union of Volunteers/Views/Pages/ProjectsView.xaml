<UserControl x:Class="Union_of_Volunteers.Views.Pages.ProjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Union_of_Volunteers.Views.Pages" 
             xmlns:pages="clr-namespace:Union_of_Volunteers.ViewModels.Pages" 
             xmlns:interface="clr-namespace:Union_of_Volunteers.Models.Interfaces" 
             d:DataContext="{d:DesignInstance Type=pages:ProjectsPageViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid>
        <Button Style="{StaticResource CancelButton}" Command="{Binding CancelButtonCommand}" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="80.5 77.1 0 0" Content="Назад" />
        <ScrollViewer Width="1000" Height="807">
            <ItemsControl ItemsSource="{Binding Projects}" >
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type interface:Api}" >
                        <Button Background="Transparent" 
                BorderThickness="0" 
                Padding="0" 
                Margin="10"
                Command="{Binding DataContext.SelectProjectCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                CommandParameter="{Binding}">
                            <Border Background="White" 
                    BorderBrush="LightGray" 
                    BorderThickness="1" 
                    CornerRadius="10" 
                    Width="300" 
                    Height="400">
                                <Border Margin="10" Padding="10" Background="White" CornerRadius="8" BorderBrush="Gray" BorderThickness="1">
                                    <StackPanel Width="456" Height="514">
                                        <Border
                                        CornerRadius="8,8,8,8" 
                                        ClipToBounds="True">
                                            <Image Source="{Binding ImageUrl}" 
                                           Height="229"
                                           Stretch="UniformToFill"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                                        </Border>
                                        <TextBlock Text="{Binding title}" FontWeight="Bold" FontSize="16"/>
                                        <TextBlock Text="{Binding description}" TextWrapping="Wrap" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Border>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
